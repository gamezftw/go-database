{
  "$schema": "https://raw.githubusercontent.com/jetify-com/devbox/0.13.7/.schema/devbox.schema.json",
  "env": {
    // "PATH":             "$DEVBOX_PROJECT_ROOT/bin/:${PATH}",
    // "XDG_CONFIG_HOME":  "$DEVBOX_CONFIG_DIR/",
    // "XDG_DATA_HOME":    "$DEVBOX_PROJECT_ROOT/.local/share",
    // "XDG_CACHE_HOME":   "$DEVBOX_PROJECT_ROOT/.cache",
  },
  "packages": [
    "go@latest",
  ],
  "shell": {
    "init_hook": [
      // "alias lvim='lvim -u $DEVBOX_CONFIG_DIR/lvim/config.lua'",
      "source <(devbox completion zsh)",
    ],
    "scripts": {
      "test": [
        "echo \"Error: no test specified\" && exit 1",
      ],
    },
  },
}
